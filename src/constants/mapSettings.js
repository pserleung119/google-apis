const mapSettings = {
  zoom: 8
}
const POINT_MARKER_ICON_CONFIG = {
}
const LINE_PATH_CONFIG = {
  strokeColor: "#FFFFFF",
  strokeOpacity: 0.8,
  strokeWeight: 1,
}

export { mapSettings, POINT_MARKER_ICON_CONFIG, LINE_PATH_CONFIG }
